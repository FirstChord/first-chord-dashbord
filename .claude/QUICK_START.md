# Claude Code repository entry

This file is only a compatibility pointer. Current agent instructions live in
[`AGENTS.md`](../AGENTS.md); the documentation router is
[`docs/README.md`](../docs/README.md), and current state is
[`docs/CURRENT_STATUS.md`](../docs/CURRENT_STATUS.md).

Do not use the older `.claude/skills/` files as production instructions. Their
historical portal-era content is superseded by `AGENTS.md`, current code/tests,
and the focused workflow and operations documents.
