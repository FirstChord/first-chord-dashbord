# Add student compatibility pointer

The old manual portal skill is retired. Use the **Waiting and onboarding** row
in [`AGENTS.md`](../../AGENTS.md), inspect the current `/admin/onboard` code and
tests, and preserve the documented partial-failure recovery boundary. Never edit
generated mapping or credential artifacts to compensate for an upstream issue.
